# DJAgent 项目排查报告

## 一、PRD 功能对照

### ✅ 已实现功能

| 功能模块 | 实现状态 | 说明 |
|----------|----------|------|
| 用户登录 | ✅ | `/api/users` 接口，用户选择登录 |
| 角色区分 | ✅ | 业务负责人 / 一线人员 |
| 风险数据展示 | ✅ | `/api/risk-data` 接口，表格展示 |
| AI对话功能 | ✅ | `/api/chat` 接口 |
| 任务创建 | ✅ | `create_task` 工具 |
| 任务下发 | ✅ | `assign_task` 工具 |
| 任务列表 | ✅ | `/api/tasks` 接口 |
| 文件上传 | ✅ | `/api/tasks/{task_id}/upload-file` |
| 聊天历史保存 | ✅ | `save_chat_message` 工具 |
| 健康检查 | ✅ | `/api/health` |

### ❌ 未实现功能

| 功能 | PRD要求 | 当前状态 |
|------|---------|----------|
| **parse_excel** | PRD 3.1.5 要求解析 Excel | ❌ 未实现 |
| **parse_pdf** | PRD 3.1.5 要求解析 PDF | ❌ 未实现 |
| **parse_word** | PRD 3.1.5 要求解析 Word | ❌ 未实现 |
| **数据筛选/搜索** | PRD 3.1.2 风险数据筛选搜索 | ❌ 无前端筛选 UI |
| **分页自定义** | PRD 3.2.1 支持5/10/20/50/100条 | ❌ 未实现 |
| **拖拽调整栏宽** | PRD 3.2.1 三栏拖拽调整 | ❌ 未实现 |
| **任务状态流转** | 已创建→已下发→反馈中→反馈完成 | ⚠️ 部分实现 |
| **响应式布局** | PRD 4.1 移动端适配 | ❌ 未实现 |
| **文件大小限制** | PRD 3.1.5 单文件<50MB | ❌ 未验证 |

---

## 二、Bug 和逻辑错误

### 🔴 严重问题

| 问题 | 位置 | 说明 |
|------|------|------|
| **allowed_tools 配置不完整** | `config.py:85-97` | 当前只配置了 2 个工具，其他工具被注释 |
| **工具名称格式错误** | `config.py` | 之前用 `list_users` 导致授权失败 |

### 🟡 中等问题

| 问题 | 位置 | 说明 |
|------|------|------|
| **Session 缓存问题** | `session_manager.py` | 修改配置后需刷新页面才能生效 |
| **缺少错误边界** | 前端 App.jsx | 网络错误无友好提示 |
| **CORS 端口硬编码** | `app_fastapi.py:48` | 5173/5174 硬编码 |
| **API 端口不一致** | README.md vs 实际 | ✅ 已统一为 5005 |

### 🟢 小问题

| 问题 | 位置 | 说明 |
|------|------|------|
| **后端启动命令复杂** | 需要 `unset CLAUDECODE` | 与 Claude Code 会话冲突 |
| **Skills 目录有 6 个** | `skills/` | 但只部分加载 |
| **降级模式未充分测试** | `HAS_AGENT_SDK=False` | 降级逻辑未验证 |

---

## 三、架构问题

1. **MCP vs Skills 混用**
   - Manager 模式用 MCP 工具
   - Staff 模式用 Skills
   - 建议统一使用 Skills 简化架构

2. **工具授权复杂**
   - 需要手动维护 `allowed_tools` 列表
   - 新增工具需同时修改 MCP 和配置

3. **配置分散**
   - `config.py` 配置
   - `mcp_server.py` 工具定义
   - `session_manager.py` 会话管理
   - 缺乏统一配置中心

---

## 四、建议优先级

### P0 - 必须修复
1. 补全 `allowed_tools` 全部 10 个工具
2. 修复工具名称格式 (`mcp__djagent_tools__xxx`)

### P1 - 应该实现
3. 添加风险数据筛选/搜索功能
4. 添加分页自定义
5. 添加文件解析工具 (Excel/PDF/Word)

### P2 - 可以优化
6. 拖拽调整栏宽
7. 移动端响应式适配
8. 统一 MCP 和 Skills 架构

---

## 五、AI工具对照

### PRD 要求的 AI 工具 (3.1.4)
- ✅ `parse_csv`: 解析CSV文件
- ❌ `parse_excel`: 解析Excel文件
- ❌ `parse_pdf`: 解析PDF文件
- ❌ `parse_word`: 解析Word文件
- ✅ `read_risk_data`: 读取风险数据
- ✅ `list_users`: 列出可用用户
- ❌ `get_task_status`: 查询任务状态
- ✅ `get_task_detail`: 获取任务详情

### 当前 MCP 工具 (10个)
- ✅ `list_users`
- ✅ `create_task`
- ✅ `assign_task`
- ✅ `get_task_detail`
- ✅ `parse_csv`
- ✅ `read_risk_data`
- ✅ `update_task_status`
- ✅ `save_chat_message`
- ✅ `save_uploaded_file`
- ✅ `list_uploaded_files`

---

## 六、前端组件对照

### PRD 6.2 业务负责人工作区
- ✅ 顶部导航栏: 标题、用户信息
- ⚠️ 用户切换（部分）
- ⚠️ 退出（实现但未完善）
- ✅ 三栏布局
- ✅ 左侧：风险数据表格、任务列表
- ✅ 中间：AI对话区
- ✅ 右侧：任务详情、反馈总结
- ❌ 拖拽调整栏宽
- ❌ 响应式适配

### PRD 6.3 一线人员工作区
- ✅ 顶部: 标题
- ✅ 全屏对话界面
- ✅ 消息气泡
- ✅ 底部输入框
- ✅ 文件上传按钮
- ✅ 发送按钮
- ⚠️ 任务自动推送（需验证）

---

生成时间: 2026-03-09
