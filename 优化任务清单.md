## 修改任务清单

### 修改1: 调整左侧栏区域高度分配

**文件**: `/Users/chenjiarui/SoftwareProject/DJAgent/frontend/src/App.css`

**位置**: 约404-412行 `.risk-data-section` 样式

**当前代码**:
```css
.risk-data-section {
  flex: 1;
  padding: var(--spacing-lg);
  border-bottom: 1px solid var(--border-color);
  overflow: auto;
  display: flex;
  flex-direction: column;
  min-height: 350px;    /* 当前设置 */
}
```

**修改为**:
```css
.risk-data-section {
  flex: 1;
  padding: var(--spacing-lg);
  border-bottom: 1px solid var(--border-color);
  overflow: auto;
  display: flex;
  flex-direction: column;
  min-height: 200px;    /* 减小到200px */
  max-height: 45%;      /* 新增：最大占45%高度 */
}
```

---

**位置**: 约651-665行 `.task-list-section` 样式

**当前代码**:
```css
.task-list-section {
  padding: var(--spacing-lg);
  overflow: hidden;
  display: flex;
  flex-direction: column;
  max-height: 180px;    /* 当前设置 */
  flex-shrink: 0;
  transition: all 0.3s ease;
}

.task-list-section.collapsed {
  max-height: 48px;
  padding: var(--spacing-sm) var(--spacing-lg);
}
```

**修改为**:
```css
.task-list-section {
  padding: var(--spacing-lg);
  overflow: hidden;
  display: flex;
  flex-direction: column;
  max-height: 300px;    /* 增大到300px */
  min-height: 120px;    /* 新增：最小高度 */
  flex-shrink: 0;
  transition: all 0.3s ease;
  flex: 1;             /* 新增：占据剩余空间 */
}

.task-list-section.collapsed {
  max-height: 40px;    /* 稍微减小 */
  padding: var(--spacing-sm) var(--spacing-lg);
}
```

---

### 修改2: 任务栏折叠按钮去掉文案，只保留图标

**文件**: `/Users/chenjiarui/SoftwareProject/DJAgent/frontend/src/App.jsx`

**位置**: 约627-629行

**当前代码**:
```jsx
<button className="collapse-btn" onClick={() => setTasksCollapsed(!tasksCollapsed)}>
  {tasksCollapsed ? '▼ 展开' : '▲ 折叠'}
</button>
```

**修改为**:
```jsx
<button 
  className="collapse-btn icon-only" 
  onClick={() => setTasksCollapsed(!tasksCollapsed)}
  title={tasksCollapsed ? '展开' : '折叠'}
>
  <span className={`collapse-icon ${tasksCollapsed ? 'collapsed' : ''}`}>
    {tasksCollapsed ? '▼' : '▲'}
  </span>
</button>
```

---

**文件**: `/Users/chenjiarui/SoftwareProject/DJAgent/frontend/src/App.css`

**位置**: 约686-698行 `.collapse-btn` 样式

**当前代码**:
```css
.collapse-btn {
  background: transparent;
  border: none;
  padding: 4px;
  border-radius: var(--radius-sm);
  font-size: 0.75rem;
  cursor: pointer;
  color: var(--text-secondary);
  transition: all 0.2s;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 28px;
  height: 28px;
}
```

**保持不变**，这个样式已经正确。

---

### 修改3: 右边栏添加折叠/展开图标，去掉关闭按钮

**文件**: `/Users/chenjiarui/SoftwareProject/DJAgent/frontend/src/App.jsx`

**位置**: 需要修改右侧面板的渲染逻辑

**当前代码** (约690-732行附近):
```jsx
<div className={`sidebar right ${rightPanelOpen ? 'open' : 'collapsed'}`}>
  {rightPanelOpen && (
    <>
      <div className="right-panel-header">
        <h3>📋 详情信息</h3>
        <button 
          className="close-panel-btn" 
          onClick={() => setRightPanelOpen(false)}
          title="关闭"
        >
          ✕
        </button>
      </div>
      {/* 内容区域 */}
    </>
  )}
</div>
```

**修改为**:
```jsx
<div className={`sidebar right ${rightPanelOpen ? 'open' : 'collapsed'}`}>
  {rightPanelOpen ? (
    <>
      <div className="right-panel-header">
        <h3>📋 详情信息</h3>
        <button 
          className="toggle-panel-btn" 
          onClick={() => setRightPanelOpen(false)}
          title="收起"
        >
          <span className="toggle-icon">▶</span>
        </button>
      </div>
      {/* 内容区域 */}
    </>
  ) : (
    <button 
      className="expand-panel-btn" 
      onClick={() => setRightPanelOpen(true)}
      title="展开详情"
    >
      <span className="expand-icon">◀</span>
      <span className="expand-text">详情</span>
    </button>
  )}
</div>
```

---

**文件**: `/Users/chenjiarui/SoftwareProject/DJAgent/frontend/src/App.css`

**位置**: 需要修改和添加右侧面板样式

**修改 `.sidebar.right` 相关样式** (约1045行附近):

当前:
```css
.sidebar.right {
  height: 100%;
  overflow-y: auto;
  background: white;
  transition: all 0.3s ease;
  border-left: 1px solid var(--border-color);
  position: relative;
}

.sidebar.right.collapsed {
  width: 0;
  opacity: 0;
  overflow: hidden;
  padding: 0;
  border-left: none;
  flex-shrink: 0;
}

.sidebar.right.open {
  width: 25%;
  opacity: 1;
  padding: var(--spacing-lg);
  flex-shrink: 0;
}
```

修改为:
```css
.sidebar.right {
  height: 100%;
  overflow-y: auto;
  background: white;
  transition: all 0.3s ease;
  border-left: 1px solid var(--border-color);
  position: relative;
}

/* 折叠状态 - 保留窄条 */
.sidebar.right.collapsed {
  width: 36px;                     /* 保留36px宽度的窄条 */
  opacity: 1;                      /* 可见 */
  overflow: hidden;
  padding: 0;
  flex-shrink: 0;
  display: flex;
  align-items: flex-start;
  justify-content: center;
  padding-top: var(--spacing-lg);
}

/* 展开状态 */
.sidebar.right.open {
  width: 25%;
  opacity: 1;
  padding: var(--spacing-lg);
  flex-shrink: 0;
}
```

**修改右侧面板头部样式**:

当前 `.right-panel-header` (如果有的话):
```css
.right-panel-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--spacing-lg);
  padding-bottom: var(--spacing-md);
  border-bottom: 2px solid var(--primary-color);
}

.right-panel-header h3 {
  font-size: 1rem;
  color: var(--text-primary);
  margin: 0;
  font-weight: 600;
}
```

添加/修改切换按钮样式:
```css
/* 收起按钮 (展开状态下的) */
.toggle-panel-btn {
  width: 28px;
  height: 28px;
  border: none;
  background: var(--bg-gray);
  border-radius: var(--radius-sm);
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.9rem;
  color: var(--text-secondary);
  transition: all 0.2s;
  padding: 0;
}

.toggle-panel-btn:hover {
  background: var(--border-color);
  color: var(--text-primary);
}

.toggle-icon {
  display: inline-block;
  font-size: 10px;
}

/* 展开按钮 (折叠状态下的窄条) */
.expand-panel-btn {
  width: 100%;
  padding: var(--spacing-sm) 0;
  border: none;
  background: transparent;
  cursor: pointer;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 4px;
  color: var(--text-secondary);
  transition: all 0.2s;
  writing-mode: vertical-rl;  /* 垂直文字 */
  text-orientation: mixed;
}

.expand-panel-btn:hover {
  color: var(--primary-color);
  background: var(--bg-gray);
}

.expand-icon {
  font-size: 10px;
  display: inline-block;
}

.expand-text {
  font-size: 0.75rem;
  writing-mode: vertical-rl;
}
```

---

## 验证清单

请确保修改了以下文件和位置:

1. **App.jsx**:
   - [ ] 添加 `rightPanelOpen` 状态 (默认 false)
   - [ ] 修改 `handleTaskClick` 函数，添加 `setRightPanelOpen(true)`
   - [ ] 修改右侧面板渲染逻辑，添加展开/收起按钮
   - [ ] 修改任务栏折叠按钮，去掉文案只保留图标

2. **App.css**:
   - [ ] 修改 `.risk-data-section` 样式，减小 min-height 和 max-height
   - [ ] 修改 `.task-list-section` 样式，增大 max-height 和 min-height
   - [ ] 修改 `.collapse-btn` 样式 (已正确，保持不变)
   - [ ] 修改 `.sidebar.right` 样式，调整 collapsed 状态保留窄条
   - [ ] 添加 `.toggle-panel-btn`、`.expand-panel-btn` 等新样式
   - [ ] 修改分页控件样式 (已完成)
   - [ ] 修改消息气泡样式 (已完成)
   - [ ] 修改输入框对齐样式 (已完成)
   - [ ] 修改表格紧凑样式 (已完成)

---

请执行以上所有修改，然后运行 `npm run build` 验证构建是否成功。

如果有任何问题，请告诉我！